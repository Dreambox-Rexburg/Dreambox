"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9ABABA",
      contrastText: "#F6F9F9",
    },
    secondary: {
      main: "#00ff00",
    },
  },
});

export default theme;
